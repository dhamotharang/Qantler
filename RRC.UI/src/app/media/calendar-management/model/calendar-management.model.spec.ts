import { CalendarManagement } from './calendar-management.model';

describe('CalendarManagement', () => {
  it('should create an instance', () => {
    expect(new CalendarManagement()).toBeTruthy();
  });
});
