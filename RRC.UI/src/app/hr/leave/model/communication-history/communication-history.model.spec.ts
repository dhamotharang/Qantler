import { CommunicationHistory } from './communication-history.model';

describe('CommunicationHistory', () => {
  it('should create an instance', () => {
    expect(new CommunicationHistory()).toBeTruthy();
  });
});
